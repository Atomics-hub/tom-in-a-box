# Smoke authorization claim

Severity: unknown

Affected file: `src/index.ts`

This is a harness smoke claim, not a real vulnerability report. In offline mode, every verification agent returns `NEEDS_REVIEW`, so the aggregate status should be `NEEDS_MANUAL_REVIEW`.
